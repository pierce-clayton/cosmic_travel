<h1><%= @mission.name %></h1>
<ul>
<li><%= link_to  @mission.scientist.name, scientist_path(@mission.scientist.id) %> </li>
<li><%= link_to  @mission.planet.name, planet_path(@mission.planet.id) %></li>
</ul>